<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Address Added Successfully - Advanced E-commerce</title>
  <style>
    /* Reset and base */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    body {
      background: #fffbee;
      color: #333;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px 15px;
    }
    h1 {
      font-size: 2.5rem;
      margin-bottom: 20px;
      color: #a1861f;
      font-weight: 700;
      text-align: center;
      letter-spacing: 1px;
      user-select: none;
    }

    /* Container for success page */
    .success-container {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 10px 30px rgba(161, 134, 31, 0.35);
      max-width: 600px;
      width: 100%;
      padding: 30px 45px;
      text-align: center;
    }
    .success-message {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 20px;
    }
    .success-message svg {
      width: 60px;
      height: 60px;
      fill: #a1861f;
    }
    .success-message h2 {
      font-size: 1.8rem;
      color: #6e5b14;
      font-weight: 700;
    }
    .success-message p {
      font-size: 1.1rem;
      color: #555;
    }

    /* Return Button */
    .btn-return {
      margin-top: 20px;
      background-color: #a1861f;
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 12px 28px;
      font-size: 1.1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 6px 10px rgba(161, 134, 31, 0.5);
      user-select: none;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
      display: inline-block;
      text-decoration: none;
    }
    .btn-return:hover,
    .btn-return:focus {
      background-color: #7e6b00;
      box-shadow: 0 8px 14px rgba(126, 107, 0, 0.7);
      outline: none;
    }

    /* Footer note */
    footer {
      margin-top: 60px;
      font-size: 0.9rem;
      color: #7e6b00;
      user-select: none;
      text-align: center;
      letter-spacing: 0.03em;
    }

    /* Responsive adjustments */
    @media (max-width: 600px) {
      body {
        padding: 12px;
      }
      h1 {
        font-size: 2rem;
        margin-bottom: 15px;
      }
      .success-container {
        padding: 20px 25px;
      }
      .success-message svg {
        width: 50px;
        height: 50px;
      }
      .success-message h2 {
        font-size: 1.5rem;
      }
      .success-message p {
        font-size: 1rem;
      }
      .btn-return {
        font-size: 1rem;
        padding: 10px 20px;
      }
    }
  </style>
</head>

<body>
  <div class="success-container">
    <div class="success-message">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24">
        <path d="M9 16.2l-3.5-3.5c-.39-.39-1.01-.39-1.4 0-.39.39-.39 1.01 0 1.4l4.19 4.19c.39.39 1.02.39 1.41 0L20.3 7.7c.39-.39.39-1.01 0-1.4-.39-.39-1.01-.39-1.4 0L9 16.2z"/>
      </svg>
      <h2>Address Added Successfully!</h2>
      <p>Your new address has been saved. You can now use it for your orders.</p>
      <a href="/user/userProfile" class="btn-return" aria-label="Return to Profile">Return to Profile</a>
    </div>
  </div>
  <footer>
    © 2024 Your E-commerce Site. All rights reserved.
  </footer>
</body>
</html>